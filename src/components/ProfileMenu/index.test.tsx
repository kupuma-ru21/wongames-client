import { renderWithTheme } from 'utils/tests/helpers';
import ProfileMenu from '.';

describe('<ProfileMenu />', () => {
  it.skip('should render the menu', () => {
    renderWithTheme(<ProfileMenu />);
  });
});
